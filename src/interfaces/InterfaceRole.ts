
type IRoleItem = {
  id?: number | null;
  name: string;
  description?: string;
  permissions?: any;
  companyId?: number;
};

type IRoleListItem = {
  id?: number;
  name: string;
  description?: string;
};
